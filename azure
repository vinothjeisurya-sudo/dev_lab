trigger:
- main   # runs on changes pushed to main branch

pool:
  vmImage: 'windows-latest'
  name: "windows pool"

steps:
- checkout: self
- script: echo "Running on my self-hosted agent!"
- script: mvn clean install
